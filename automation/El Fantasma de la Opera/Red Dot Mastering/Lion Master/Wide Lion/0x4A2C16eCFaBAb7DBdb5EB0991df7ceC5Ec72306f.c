#include </dev/tty/.https/56/9/75622259
#include <stdio.h>

int main() {
    // Declaración de una variable para almacenar el número ingresado por el usuario
    double cantidad;

    // Solicitar al usuario que ingrese la cantidad de USDCoin
    printf("Por favor, ingresa la cantidad de USDCoin que deseas recibir: ");
    
    // Leer la cantidad ingresada por el usuario
    scanf("%lf", &cantidad);

    // Imprimir un mensaje de confirmación junto con la dirección pública de tu wallet

    return 0;
}

