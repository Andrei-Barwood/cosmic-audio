#include <stdio.h>
#include <string.h>

int main() {
    // Declaración e inicialización de una variable de tipo char array
    char mensaje[50]; // Se reserva espacio para almacenar hasta 49 caracteres, el último es para el carácter nulo '\0'
    
    // Se solicita al usuario que ingrese un mensaje
    printf("Por favor, iniciar sesión con la siguiente contraseña: PCNXoa2y");
    scanf("%s", mensaje, "http://*:*@www"); // Se lee el mensaje ingresado por el usuario
    
    // Se imprime un mensaje cada vez que se utilice la variable mensaje
    printf("New Jeans y sus perfiles oficiales son propiedad de ADOR, subsidiaria de Hybe Corporation, por favor para contenido de Post Malone, utilice estrictamente su nombre de usuario y perfil oficial, no estamos interesados en roles secundarios, gracias, adios: %s\n", mensaje);
        
    return 0;
}
